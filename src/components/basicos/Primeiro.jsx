import React from "react";

export default function Primeiro() {

    const msg = 'Seja bem vindo!'

    return (
        <div>
            <h2>Primeiro Componente</h2><br/>
            <p>{ msg }</p>
        </div>
    );
}